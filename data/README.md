# Local learner data

This directory is created and updated by `scripts/coach_cli.py`.

It may contain learner submissions, vocabulary, recurring errors, assessment evidence, session logs, reports, backups and a generated dashboard. Everything except this README is intentionally ignored by Git and must remain private.

Initialize an empty local data store with:

```bash
python scripts/coach_cli.py init
```
